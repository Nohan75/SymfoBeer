{% extends 'base.html.twig' %}

{% block title %}Brewerie{% endblock %}

{% block body %}
    <h1>Brewerie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brewerie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brewerie.name }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ brewerie.adress }}</td>
            </tr>
            <tr>
                <th>Brand</th>
                <td>{{ brewerie.brand }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ brewerie.city }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ brewerie.state }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ brewerie.country }}</td>
            </tr>
            <tr>
                <th>Zipcode</th>
                <td>{{ brewerie.zipcode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('brewerie_index') }}">back to list</a>

    <a href="{{ path('brewerie_edit', {'id': brewerie.id}) }}">edit</a>

    {{ include('brewerie/_delete_form.html.twig') }}
{% endblock %}
