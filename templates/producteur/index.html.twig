{% extends 'base.html.twig' %}

{% block title %}Producteur index{% endblock %}
{% block page %}Producer{% endblock %}
{% block menu %}
    <ul class="hidden md:flex px-4 mx-auto font-semibold font-heading space-x-12">
        <li><a class="hover:text-gray-200" href="{{ path('home') }}">Home</a></li>
        <li><a class="hover:text-gray-200" href="{{ path('beerview') }}">Beer</a></li>
        <li><a class="hover:text-gray-200" href="{{ path('client_index') }}">Client</a></li>
    </ul>
{% endblock %}


{% block body %}
    <h1 class="font-serif font-medium text-2xl">Producteur index</h1>
    <br>
    <table class="table-auto">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Email</th>
                <th>Photo</th>
                <th class="w-1/5">DateOfBirth</th>
            </tr>
        </thead>
        <tbody>
        {% for producteur in producteurs %}
            <tr>
                <td>{{ producteur.id }}</td>
                <td>{{ producteur.name }}</td>
                <td>{{ producteur.email }}</td>
                <td><img src="{{ producteur.photo }}"></td>
                <td>{{ producteur.dateOfBirth|date("m/d/Y") }}</td>
                <td>
                    <a href="{{ path('producteur_show', {'id': producteur.id}) }}"><button class="rounded-full py-1 px-2 border-2 border-black">show</button></a>
                    <a href="{{ path('producteur_edit', {'id': producteur.id}) }}"><button class="rounded-full py-1 px-2 border-2 border-black">edit</button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <br>

    <a href="{{ path('producteur_new') }}"><button class="rounded-full py-1 px-2 border-2 border-black" > Create new </button></a>
{% endblock %}
