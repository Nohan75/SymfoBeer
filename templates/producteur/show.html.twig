{% extends 'base.html.twig' %}

{% block title %}Producteur{% endblock %}

{% block body %}
    <h1>Producteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producteur.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ producteur.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ producteur.email }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ producteur.photo }}</td>
            </tr>
            <tr>
                <th>DateOfBirth</th>
                <td>{{ producteur.dateOfBirth }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('producteur_index') }}">back to list</a>

    <a href="{{ path('producteur_edit', {'id': producteur.id}) }}">edit</a>

    {{ include('producteur/_delete_form.html.twig') }}
{% endblock %}
